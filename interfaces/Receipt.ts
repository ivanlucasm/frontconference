export interface IReceipt {
    id: string
    receipt_key: string
    receipt_number: string
    receipt_serie: string
    merchant_origin: string
    employee_id: string
    created_at: string
    updated_at: string
}
